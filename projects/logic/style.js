class Style {
  constructor() {
    this.stroke = color(0);
    this.strokeWeight = 3;
    this.fill = color(255);

    this.strokeHover = color(50);
    this.strokeWeightHover = 3;
    this.fillHover = color(220);

    this.strokeOn = color(255, 0, 0);
    this.strokeWeightOn = 3;
    this.fillOn = color(255);

    this.strokeOnHover = color(220, 0, 0);
    this.strokeWeightOnHover = 3;
    this.fillOnHover = color(220);
  }
}
